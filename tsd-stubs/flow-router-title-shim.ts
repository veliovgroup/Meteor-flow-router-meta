import type { Router } from './flow-router-extra-shim';

/**
 * Minimal stub for `meteor/ostrio:flow-router-title` re-export checks in tsd.
 */
export declare class FlowRouterTitle {
  constructor(router: Router);
  set(title: string): boolean;
}
